<div class="p-6 bg-black min-h-screen text-white">
  <div class="max-w-7xl mx-auto">
    <header class="flex justify-between items-center mb-10">
      <h1 class="text-3xl font-bold">Admin <span class="text-primary">Dashboard</span></h1>
      <.link href={~p"/products"} class="btn btn-outline btn-sm text-primary">
    View Storefront
  </.link>
      <div class="badge badge-primary p-4">Admin Mode</div>

    </header>

    <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-10">
      <div class="bg-base-100/50 p-6 rounded-2xl border border-white/10">
        <p class="text-gray-400">Pending Orders</p>
        <p class="text-4xl font-black text-primary"><%= @pending_count %></p>
      </div>
      </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      <.link href={~p"/admin/products"} class="btn btn-outline btn-lg h-32 rounded-3xl flex flex-col gap-2">
        <span class="text-2xl">🍔</span> Manage Products
      </.link>
      <.link href={~p"/admin/orders"} class="btn btn-outline btn-lg h-32 rounded-3xl flex flex-col gap-2">
        <span class="text-2xl">📦</span> Active Orders
      </.link>
      <.link href={~p"/admin/reports"} class="btn btn-outline btn-lg h-32 rounded-3xl flex flex-col gap-2">
        <span class="text-2xl">📈</span> View Reports
      </.link>
    </div>
  </div>
</div>